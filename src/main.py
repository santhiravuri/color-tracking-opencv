from color_tracker import ColorTracker  # Import the ColorTracker class

if __name__ == "__main__":  # Check if this script is being run as the main program
    tracker = ColorTracker()  # Create a ColorTracker instance
    tracker.track_color()  # Start tracking the color

